#include "PhoneBook.hpp"
#include <iostream>



int main(void)
{
    PhoneBook pb = PhoneBook();
    while (true)
    {
        std::string cmd;
        
        std::cout << "Please input the command:" << std::endl;
        std::cin >> cmd;

        if (cmd == "ADD") {

        }


    }
}